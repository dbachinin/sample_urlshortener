<%= form_for(inbox) do |f| %>
  <% if inbox.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inbox.errors.count, "error") %> prohibited this inbox from being saved:</h2>

      <ul>
      <% inbox.errors.full_messages.each do |message| %>

        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<div class="col-lg-offset-4 form-group">
  <div class="field">
    <%= f.label :shortmessage %>
    <%= f.text_field :shortmessage, {:value => @url} %>
  </div>

   <div class="controls">
    <%= f.label :recipients %>

    <%= f.select(:reseiver, User.all.collect {|p| [ p.email, p.id ] })%>
   </div>
   </div>



  </div>

  <div class="actions col-xs-12 col-sm-offset-5">
    <%= f.submit "Share", class: "btn btn-primary"  %>
  </div>

<% end %>
