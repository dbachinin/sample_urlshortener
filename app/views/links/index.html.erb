 <div class="title">
  <h1>My links</h1>
  
</div>

  <%
    @link.each do |link|
#    ui = @link2.user_id.to_i
#    ue = User.find(ui).email
    if link.user_id == @user.id
    
    email = @user.email 
    title = link.title 
    @display_slug = link.display_slug 
    created_at = link.created_at 
    given_url = link.given_url 
    slug = link.slug 
   %>



    <div class="col-xs-4 col-xs-5">
          <div class="well">

    <li><span class="title"><%= truncate(title, length: 40) %></span></li> 
      <ul class="click_time">
      <p><span class="time"><%= time_ago_in_words(created_at)  %> ago </span></p>
      </ul> 
    <p class="short_url"><%= link_to @display_slug, @display_slug  %></p>

      <p class="long_url"><%= link_to truncate(given_url, length: 50), given_url  %></p>
      <div class = class="col-xs-4">
      <li style= "height:45px;"><%= button_to "Delete", link, method: :delete, data: { confirm: "Are you sure you wish to delete #{link.slug}?" }, class: "btn btn-danger" %></li>

      <li ><span><%= link_to 'Share', new_inbox_path(:url => link.id), class: 'btn btn-default' %></span></li>

      </div>
    </div>
    </div>



    <% end %>
  <% end %>
</div>
</div>

<p><%= link_to 'New link', '/links/new',class: "btn-lg btn-primary" %></p>
</div>
